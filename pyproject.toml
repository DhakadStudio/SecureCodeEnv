
[project]
dependencies = [
    "openenv-core>=0.2.0"
]
name = "SecureCodeEnv"
version = "0.1.0"
description = "Hackathon project"

dependencies = [
    "fastapi",
    "uvicorn",
    "openai"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
